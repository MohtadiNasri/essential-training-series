events {}

http {
  upstream api_cluster {
    server api:3000;    # Compose DNS resolves 'api' to all replicas
  }

  server {
    listen 80;

    location / {
      proxy_pass http://api_cluster;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
    }
  }
}
